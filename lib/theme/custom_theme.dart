import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';


class CustomeTheme{
final mainPadding =const EdgeInsets.all(8);
final pagePadding =const EdgeInsets.fromLTRB(
    26, 0, 27, 0); //left and right padding for main container used in pages

final mainFont = GoogleFonts.montserrat; // font for every text

final primaryColor =const Color(0xFF073763); // main color
final mainHeight = 60.0; //height for textfield and buttons
final mainWidth = 378.0; //width for textfield and buttons
final fontColor = const Color(0xFF454545); //fontcolor for text
final boarderCircularSize = 10.0;

}

